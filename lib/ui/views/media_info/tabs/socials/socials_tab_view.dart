import 'package:flutter/material.dart';

class SocialsTab extends StatelessWidget {
  const SocialsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('SOCIALS TAB'),
    );
  }
}
