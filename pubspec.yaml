name: aniyoka
description: a flutter project for mobile development.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  stacked: ^3.4.0
  stacked_services: ^1.1.0
  graphql_flutter: ^5.3.0
  google_fonts: ^6.2.1
  palette_generator: ^0.3.3
  cached_network_image: ^3.3.1

dev_dependencies:
  build_runner: ^2.4.5
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mockito: ^5.4.1
  stacked_generator: ^2.0.4
  golden_toolkit: ^0.15.0

flutter:
  uses-material-design: true

